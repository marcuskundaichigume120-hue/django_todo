services:
  - type: web
    name: django-todo
    env: python
    buildCommand: "pip install -r requirements.txt && python3 manage.py collectstatic --noinput"
    startCommand: "gunicorn todoproject.wsgi:application --bind 0.0.0.0:$PORT"